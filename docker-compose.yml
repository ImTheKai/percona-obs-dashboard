services:
  backend:
    build: ./backend
    ports:
      - "4000:4000"
    volumes:
      - ./data:/data
    env_file: .env

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    depends_on:
      - backend
